<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>New Taste Kitchen</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  </head>
  <style>
        /* Custom Styling */
        .top-bar {
            background-color: #043927; /* Darker Green */
            padding: 10px 0;
        }
        .top-bar a {
            color: white;
            text-decoration: none;
            margin: 0 10px;
            font-weight: bold;
        }
        .navbar-custom {
            background-color: #064635; /* Dark Green Background */
            padding: 15px 0;
        }
        .nav-link {
            font-size: 18px; /* Larger text */
            padding: 15px 20px; /* More spacing */
            color: white !important;
        }
        .nav-item {
            border-right: 2px solid rgba(255, 255, 255, 0.3); /* Line between items */
        }
        .nav-item:last-child {
            border-right: none;
        }
        .btn-custom {
            background-color: #FFD700; /* Gold Button */
            color: black;
            font-weight: bold;
        }

        /* Card Hover Effect */
    .card:hover {
        transform: translateY(-10px); /* Slight lift effect */
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15); /* Add shadow for a 3D effect */
    }
    /* Button Hover Effect */
    .btn-outline-success:hover {
        background-color: #198754;
        color: white;
    }

     /* Card Styling with Green Border and Shadow */
     .card {
        border-radius: 12px;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        position: relative;
        background-color: #fff;
        overflow: hidden;
        border: 2px solid #198754;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }

    /* CEO Section Styling */
.ceo-section {
    background-color: #fff;
    padding: 30px;
    border-radius: 12px;
    box-shadow: 0 4px 8px rgba(0,0,0,0.1);
}

    /* Award Cards */
.card img {
    border-top-left-radius: 12px;
    border-top-right-radius: 12px;
}
    </style>
  <body>

</header>

